version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    
    command: sleep infinity

    volumes:
      - ../:/workspaces/portfolio:cached
    
    ports:
      - "4321:4321"